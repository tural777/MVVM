<Window x:Class="MVVM.Views.ExampleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen">
    <!--<Window.CommandBindings>
        <CommandBinding x:Name="Help" Command="Help" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"></CommandBinding>
    </Window.CommandBindings>-->
    
    <StackPanel>
        <Button Command="{Binding SaveCommand}" FontSize="45">Save</Button>
        <Button Command="{Binding EditCommand}" FontSize="45">Edit</Button>
        <TextBox Name="txt" FontSize="45"></TextBox>
        
    </StackPanel>
</Window>
