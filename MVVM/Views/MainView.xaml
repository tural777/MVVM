<Window x:Class="MVVM.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM.Views" 
        xmlns:ViewModels="clr-namespace:MVVM.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <ViewModels:MainViewModel></ViewModels:MainViewModel>
    </Window.DataContext>
    
    <StackPanel HorizontalAlignment="Center">

        <TextBlock Margin="7 7 7 0" FontSize="36">Make</TextBlock>
        <TextBlock Margin="7 7 7 0" FontSize="36" Text="{Binding Car.Make}"></TextBlock>

        <TextBlock Margin="7 7 7 0" FontSize="36">Model</TextBlock>
        <TextBlock Margin="7 7 7 0" FontSize="36" Text="{Binding Car.Model}"></TextBlock>

        <TextBlock Margin="7 7 7 0" FontSize="36">Year</TextBlock>
        <TextBlock Margin="7 7 7 0" FontSize="36" Text="{Binding Car.Year}"></TextBlock>


        <Button Command="{Binding ShowCommand}" FontSize="36">Show</Button>
    </StackPanel>
</Window>
